# Hermes adapter

Read `AGENTS.md` first. Then read `CONTEXT.md`. When resuming, pausing, or transferring a task, read and update `.agents/handoffs/current.md` using `.agents/handoffs/README.md`.

Hermes retains its runtime and tool authority. It follows the repository handoff protocol for project continuity and must not read credentials or bypass the product's approval model.
